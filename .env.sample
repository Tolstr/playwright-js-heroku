API_BASE_URL=https://conduit.bondaracademy.com
# Optional if your tests use a token header:
API_TOKEN=REPLACE_WITH_FAKE_OR_OMIT


# local real env — do NOT commit this
cp .env.sample .env
# now edit .env with your real values if needed